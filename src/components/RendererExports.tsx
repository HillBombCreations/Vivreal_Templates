'use client';

export {
  HeroSectionShowcase,
  CTASectionTemplate,
  TeamPage,
  // Iter 5 — Restaurant additions (requires site-renderer >= 0.3.0 with Iter-5 bump)
  MenuPage,
  FloatingCta,
  HeroSplit,
  // Iter 6 — Services + Portfolio additions (requires site-renderer bump for Iter-6)
  PricingTiers,
  ProcessSteps,
  ProjectGallery,
  ClientLogoWall,
  TrustBadges,
  CaseStudyDetail,
} from '@hillbombcreations/site-renderer';
